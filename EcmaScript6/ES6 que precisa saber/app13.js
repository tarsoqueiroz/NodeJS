/**
 * Created by USER: tarso.
 * On         DATE: 11/08/17.
 * By         NAME: app13.js.
 */

'use strict';

const sum1 = (x, y) => {
  return x + y
}
const sum2 = (x, y) => x + y

console.log(sum1(1, 2)); // 3
console.log(sum2(1, 2)); // 3