{
  "name": "webauth.db",
  "version": "1.0.0",
  "description": "Simple JsonDB server for accounts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch ./db.json --port 5000"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
